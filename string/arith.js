const add = (a,b) => a+b;
const sub = (a,b) => a-b;
const mul = (a,b) => a*b;
const div = (a,b) => a/b;
const sqrt = (a) => Math.sqrt(a)
const max = (a,b,c,d,e,f) => Math.max(a)

module.exports = {
    add,
    sub,
    mul,
    div,
    sqrt,
    max
}
